{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Simulator",
            "type": "shell",
            "command": "cmake -B build -G Ninja && cmake --build build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}